# ==============================================================================
# 🐍 Snake Game Backend — Python Dependencies
# ==============================================================================
# Установка: pip install -r requirements.txt
# ==============================================================================

# FastAPI — современный веб-фреймворк для создания API
# Почему FastAPI: быстрый, простой, автодокументация (Swagger UI)
fastapi==0.109.2

# Uvicorn — ASGI сервер для запуска FastAPI
# Почему Uvicorn: быстрый, асинхронный, стандарт для FastAPI
uvicorn[standard]==0.27.1

# SQLAlchemy — ORM для работы с базой данных
# Почему SQLAlchemy: популярный, мощный, работает с SQLite
sqlalchemy==2.0.25

# Pydantic — валидация данных (уже входит в FastAPI, но укажем явно)
pydantic==2.6.1

# Python-dotenv — загрузка переменных из .env файла
python-dotenv==1.0.1
