# 🐍 Snake Game — Cursor AI Rules

> Проект: Классическая игра "Змейка" с современным дизайном
> Архитектура: Backend (Python/FastAPI) + Frontend (React/Vite)
> База данных: SQLite (локальная, файл snake.db)

---

## 📋 Контекст проекта

### Что это за проект?
Браузерная игра "Змейка" с:
- Красивым неоновым дизайном и анимациями
- Таблицей лидеров (хранится в SQLite)
- Локальным запуском (без деплоя на сервер)

### Целевая аудитория разработки
- Пользователь НЕ разработчик
- Нужны подробные объяснения терминов
- Нужны простые инструкции по запуску

---

## 🏗️ Архитектура

### Backend (папка `backend/`)
- **Фреймворк:** FastAPI (Python 3.10+)
- **База данных:** SQLite через SQLAlchemy
- **Порт:** 8000
- **API endpoints:**
  - `POST /api/scores` — сохранить результат
  - `GET /api/scores` — получить таблицу лидеров
  - `GET /api/scores/best` — лучший результат

### Frontend (папка `frontend/`)
- **Фреймворк:** React 18+ с Vite
- **Стилизация:** CSS Modules или styled-components
- **Порт:** 5173 (dev) / любой (production)
- **Игровой движок:** Canvas API

### Связь Frontend ↔ Backend
- REST API через fetch/axios
- CORS настроен на backend для localhost

---

## 📁 Структура файлов

```
Snake/
├── backend/
│   ├── app/
│   │   ├── __init__.py
│   │   ├── main.py           # FastAPI app, CORS, роутеры
│   │   ├── database.py       # SQLite connection
│   │   ├── models.py         # SQLAlchemy models
│   │   └── schemas.py        # Pydantic schemas
│   ├── requirements.txt
│   └── snake.db              # SQLite (auto-created)
│
├── frontend/
│   ├── src/
│   │   ├── components/
│   │   │   ├── Game.jsx      # Главный игровой компонент
│   │   │   ├── Snake.jsx     # Отрисовка змейки
│   │   │   ├── Food.jsx      # Отрисовка еды
│   │   │   ├── Score.jsx     # Текущий счёт
│   │   │   ├── GameOver.jsx  # Экран окончания
│   │   │   └── Leaderboard.jsx
│   │   ├── hooks/
│   │   │   ├── useGameLoop.js    # Игровой цикл
│   │   │   ├── useSnake.js       # Логика змейки
│   │   │   └── useKeyboard.js    # Управление
│   │   ├── api/
│   │   │   └── scores.js     # API вызовы
│   │   ├── styles/
│   │   │   └── game.css      # Стили игры
│   │   ├── App.jsx
│   │   └── main.jsx
│   ├── index.html
│   ├── package.json
│   └── vite.config.js
│
├── Docs/                     # Документация (уже есть)
├── .cursorrules              # Этот файл
├── .gitignore
├── .flake8
└── README.md
```

---

## 🎨 Дизайн-система

### Цветовая палитра (Neon/Cyberpunk)
```css
--bg-primary: #0a0a0f;        /* Тёмный фон */
--bg-secondary: #12121a;      /* Игровое поле */
--snake-head: #00ff88;        /* Голова змейки - яркий зелёный */
--snake-body: #00cc6a;        /* Тело змейки - зелёный градиент */
--snake-tail: #009955;        /* Хвост - темнее */
--food: #ff3366;              /* Еда - розовый/красный */
--food-glow: #ff336680;       /* Свечение еды */
--text-primary: #ffffff;      /* Основной текст */
--text-accent: #00ff88;       /* Акцентный текст */
--border-glow: #00ff8840;     /* Свечение границ */
```

### Типографика
- **Заголовки:** 'Orbitron', sans-serif (футуристичный)
- **Текст:** 'Rajdhani', sans-serif (читаемый, современный)
- **Счёт:** 'Press Start 2P', cursive (ретро-игровой)

### Анимации
- Появление еды: pulse + scale
- Поедание: particle burst effect
- Game Over: shake + fade
- Счёт: bounce при увеличении
- Змейка: плавное движение, градиент по телу

---

## 💻 Правила кодирования

### Python (Backend)
- Python 3.10+
- Type hints обязательны
- Docstrings для всех функций
- Async/await для FastAPI endpoints
- Pydantic для валидации данных

### JavaScript/React (Frontend)
- ES6+ синтаксис
- Функциональные компоненты + hooks
- Именование: PascalCase для компонентов, camelCase для функций
- JSDoc комментарии для сложной логики
- Отдельные файлы для каждого компонента

### Общее
- Осмысленные имена переменных (на английском)
- Комментарии на русском для пользователя
- Подробные console.log для отладки

---

## 🎮 Игровая логика

### Константы игры
```javascript
GRID_SIZE: 20,        // Размер сетки (20x20 клеток)
CELL_SIZE: 25,        // Размер одной клетки в пикселях
INITIAL_SPEED: 150,   // Начальная скорость (мс между шагами)
SPEED_INCREMENT: 5,   // Ускорение за каждую съеденную еду
MIN_SPEED: 50,        // Максимальная скорость
```

### Управление
- Стрелки или WASD — движение
- Space — пауза/продолжение
- Enter — начать заново (после Game Over)

### Логика столкновений
- Стены: Game Over (классический режим)
- Своё тело: Game Over
- Еда: +1 к длине, +10 к счёту

---

## 🚀 Команды запуска

### Backend
```bash
cd backend
python -m venv venv
venv\Scripts\activate      # Windows
pip install -r requirements.txt
uvicorn app.main:app --reload --port 8000
```

### Frontend
```bash
cd frontend
npm install
npm run dev
```

### Открыть игру
```
http://localhost:5173
```

---

## ⚠️ Важные правила для AI-агентов

1. **Объясняй термины** — пользователь не разработчик
2. **Давай пошаговые инструкции** — как запустить, что делать
3. **Не усложняй** — минимум зависимостей, простые решения
4. **Тестируй локально** — всё должно работать на Windows
5. **Комментируй код** — особенно сложные места
6. **Красивый UI важен** — это игра, визуал критичен

---

## 📚 Документация

- `Docs/dev-playbook.md` — общие правила разработки
- `Docs/nakama_api_*.md` — документация внешнего API (если понадобится)

---

## 🔄 Git Workflow

**Режим:** Pet Project (напрямую в main)

```bash
git add .
git commit -m "feat: описание"
git push
```
