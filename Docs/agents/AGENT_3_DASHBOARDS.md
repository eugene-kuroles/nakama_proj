# 📊 AGENT 3 — Frontend Dashboards

## Миссия
Создать дашборды для трёх ролей: CEO, РОП, Менеджер + общие компоненты графиков и карточек.

## Технологии
- React 18
- Recharts (графики)
- TailwindCSS
- shadcn/ui

## Задачи в порядке выполнения

### 1. Общие компоненты

**Карточки KPI:**
```
components/cards/
├── KPICard.tsx              # Число + тренд (↑↓)
├── TrendCard.tsx            # Линейный мини-график
├── ScoreCard.tsx            # Оценка с цветом (зел/желт/красн)
├── ManagerCard.tsx          # Карточка менеджера
└── CallCard.tsx             # Карточка звонка
```

**Графики:**
```
components/charts/
├── LineChart.tsx            # Динамика по времени
├── BarChart.tsx             # Сравнения
├── RadarChart.tsx           # Профиль менеджера
├── GaugeChart.tsx           # KPI индикатор
├── HeatMap.tsx              # Корреляции
└── AreaChart.tsx            # Тренды с заполнением
```

**Таблицы:**
```
components/tables/
├── CallsTable.tsx           # Таблица звонков
├── LeaderboardTable.tsx     # Рейтинг менеджеров
├── CriteriaTable.tsx        # Оценки по критериям
└── DataTable.tsx            # Базовая таблица (shadcn)
```

### 2. CEO Dashboard — Executive Overview

**Путь:** `app/(dashboard)/executive/page.tsx`

**Макет:**
```
┌────────────────────────────────────────────────────────────────┐
│  Executive Overview                           [Period: Week ▼] │
├────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐       │
│  │ Avg %    │  │ Calls    │  │ Duration │  │ Managers │       │
│  │ 78.5%    │  │ 156      │  │ 42h 15m  │  │ 8        │       │
│  │ ↑ +3.2%  │  │ ↑ +12    │  │ ↓ -2h    │  │ → 0      │       │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘       │
│                                                                 │
│  ┌────────────────────────────────┐  ┌────────────────────────┐│
│  │ Quality Trend (Last 8 weeks)   │  │ Top Issues             ││
│  │ [Line Chart]                   │  │ 1. Выявление потреб... ││
│  │                                │  │ 2. Отработка возраж... ││
│  │                                │  │ 3. Призыв к действ...  ││
│  └────────────────────────────────┘  └────────────────────────┘│
│                                                                 │
│  ┌────────────────────────────────────────────────────────────┐│
│  │ Team Performance by Manager                                 ││
│  │ [Horizontal Bar Chart]                                      ││
│  └────────────────────────────────────────────────────────────┘│
│                                                                 │
└────────────────────────────────────────────────────────────────┘
```

**Компоненты:**
- `ExecutiveSummary.tsx` — 4 KPI карточки
- `QualityTrendChart.tsx` — Линейный график по неделям
- `TopIssues.tsx` — Список проблемных критериев
- `TeamBarChart.tsx` — Рейтинг менеджеров

### 3. РОП Dashboard — Team Performance

**Путь:** `app/(dashboard)/team/page.tsx`

**Макет:**
```
┌────────────────────────────────────────────────────────────────┐
│  Team Performance                            [Period: Week ▼]  │
├────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ LEADERBOARD                                               │  │
│  │ ┌────┬─────────────────┬───────┬────────┬───────────────┐│  │
│  │ │ #  │ Manager         │ Score │ Calls  │ Trend         ││  │
│  │ ├────┼─────────────────┼───────┼────────┼───────────────┤│  │
│  │ │ 🥇 │ Иванов А.       │ 89.2% │ 24     │ ↑ +5.1%       ││  │
│  │ │ 🥈 │ Петров В.       │ 85.7% │ 31     │ ↑ +2.3%       ││  │
│  │ │ 🥉 │ Сидорова М.     │ 82.1% │ 18     │ → +0.1%       ││  │
│  │ │ 4  │ Козлов И.       │ 76.4% │ 22     │ ↓ -3.2%       ││  │
│  │ └────┴─────────────────┴───────┴────────┴───────────────┘│  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                 │
│  ┌─────────────────────────────┐  ┌─────────────────────────┐  │
│  │ Criteria Analysis           │  │ Coaching Queue          │  │
│  │ [HeatMap: Managers x Crit.] │  │ • Козлов: Возражения   │  │
│  │                             │  │ • Новикова: Закрытие   │  │
│  │                             │  │ • Смирнов: Презентация │  │
│  └─────────────────────────────┘  └─────────────────────────┘  │
│                                                                 │
└────────────────────────────────────────────────────────────────┘
```

**Компоненты:**
- `LeaderboardTable.tsx` — Рейтинг с трендами
- `CriteriaHeatMap.tsx` — Тепловая карта менеджеры × критерии
- `CoachingQueue.tsx` — Очередь на коучинг
- `TeamTrendChart.tsx` — Динамика команды

### 4. Менеджер Dashboard — My Performance

**Путь:** `app/(dashboard)/my/page.tsx`

**Макет:**
```
┌────────────────────────────────────────────────────────────────┐
│  My Performance — Иванов Алексей             [Period: Week ▼]  │
├────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐       │
│  │ My Score │  │ My Calls │  │ Rank     │  │ vs Team  │       │
│  │ 89.2%    │  │ 24       │  │ #1 / 8   │  │ +10.7%   │       │
│  │ ↑ +5.1%  │  │ ↑ +3     │  │ ↑ +2     │  │ ↑ +2.3%  │       │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘       │
│                                                                 │
│  ┌────────────────────────────┐  ┌────────────────────────────┐│
│  │ My Criteria Profile        │  │ My Trend                   ││
│  │ [Radar Chart]              │  │ [Line Chart]               ││
│  │                            │  │                            ││
│  └────────────────────────────┘  └────────────────────────────┘│
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ GROWTH AREAS (критерии для улучшения)                     │  │
│  │ ┌──────────────────────────────────────────────────────┐ │  │
│  │ │ ⚠️ Отработка возражений — 65% (команда: 72%)         │ │  │
│  │ │ 💡 Рекомендация: использовать технику SPIN...        │ │  │
│  │ └──────────────────────────────────────────────────────┘ │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ RECENT CALLS                                              │  │
│  │ ┌────────┬────────────────┬───────┬─────────────────────┐│  │
│  │ │ Date   │ Client         │ Score │ Actions             ││  │
│  │ ├────────┼────────────────┼───────┼─────────────────────┤│  │
│  │ │ 19 янв │ ООО Рога       │ 92%   │ [View]              ││  │
│  │ │ 18 янв │ ИП Сидоров     │ 78%   │ [View]              ││  │
│  │ └────────┴────────────────┴───────┴─────────────────────┘│  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                 │
└────────────────────────────────────────────────────────────────┘
```

**Компоненты:**
- `MyKPICards.tsx` — Личные показатели
- `CriteriaRadar.tsx` — Радар профиль
- `MyTrendChart.tsx` — Личная динамика
- `GrowthAreas.tsx` — Зоны роста с рекомендациями
- `RecentCallsTable.tsx` — Последние звонки

### 5. Страница деталей звонка

**Путь:** `app/(dashboard)/call/[id]/page.tsx`

**Макет:**
```
┌────────────────────────────────────────────────────────────────┐
│  Call Details — ООО Рога и Копыта              [Back to List]  │
├────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐       │
│  │ Score    │  │ Duration │  │ Date     │  │ Manager  │       │
│  │ 92%      │  │ 12:34    │  │ 19.01.25 │  │ Иванов   │       │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘       │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ CRITERIA SCORES                                           │  │
│  │ ┌──────────────────────────────────────┬───────┬────────┐│  │
│  │ │ Criteria                             │ Score │ Status ││  │
│  │ ├──────────────────────────────────────┼───────┼────────┤│  │
│  │ │ 1. Приветствие и установление контак │   5   │   ✅   ││  │
│  │ │ 2. Выявление потребностей            │   4   │   ⚠️   ││  │
│  │ │ 3. Презентация решения               │   5   │   ✅   ││  │
│  │ └──────────────────────────────────────┴───────┴────────┘│  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ DETAILED FEEDBACK (expandable)                            │  │
│  │ ▼ 2. Выявление потребностей — 4/5                        │  │
│  │   Reason: Частично выявил потребности, не уточнил бюджет │  │
│  │   Quote: "Какой продукт вас интересует?"                 │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                 │
└────────────────────────────────────────────────────────────────┘
```

## Цветовая индикация

```typescript
const getScoreColor = (percent: number) => {
  if (percent >= 85) return 'text-green-600 bg-green-100';      // Excellent
  if (percent >= 70) return 'text-yellow-600 bg-yellow-100';    // Good
  if (percent >= 50) return 'text-orange-600 bg-orange-100';    // Warning
  return 'text-red-600 bg-red-100';                             // Danger
};

const getTrendIcon = (change: number) => {
  if (change > 1) return <TrendingUp className="text-green-600" />;
  if (change < -1) return <TrendingDown className="text-red-600" />;
  return <Minus className="text-gray-400" />;
};
```

## Критерии готовности
- [ ] CEO Dashboard отображает данные
- [ ] РОП Dashboard с Leaderboard работает
- [ ] Менеджер Dashboard показывает личные KPI
- [ ] Страница деталей звонка работает
- [ ] Все графики рендерятся
- [ ] Цветовая индикация работает

## Зависимости от других агентов
- Agent 1: API эндпоинты для данных
- Agent 2: Layout, навигация, тема
- Agent 4: Расчёт метрик и KPI
