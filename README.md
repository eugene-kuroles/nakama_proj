# 🐍 Snake Game

> Классическая игра "Змейка" с современным неоновым дизайном

![Snake Game](https://img.shields.io/badge/Game-Snake-00ff88?style=for-the-badge)
![Python](https://img.shields.io/badge/Backend-Python%203.10+-blue?style=for-the-badge)
![React](https://img.shields.io/badge/Frontend-React%2018-61dafb?style=for-the-badge)

---

## 🎮 Что это?

Браузерная игра "Змейка" с:
- 🎨 Красивым неоновым дизайном в стиле Cyberpunk
- 🏆 Таблицей лидеров (сохраняется в базе данных)
- ⚡ Ускорением по мере роста змейки
- 📱 Адаптивным дизайном

---

## 🚀 Быстрый старт (для не-разработчиков)

### Что вам понадобится:

1. **Python 3.10+** — [скачать](https://www.python.org/downloads/)
2. **Node.js 18+** — [скачать](https://nodejs.org/)

### Шаг 1: Запуск Backend (сервер)

Откройте **первый** терминал (командную строку):

```bash
# Перейти в папку backend
cd backend

# Создать виртуальное окружение (изолированная среда Python)
python -m venv venv

# Активировать виртуальное окружение
# Windows (PowerShell):
.\venv\Scripts\Activate.ps1
# Windows (CMD):
venv\Scripts\activate.bat
# Linux/Mac:
source venv/bin/activate

# Установить зависимости
pip install -r requirements.txt

# Запустить сервер
uvicorn app.main:app --reload --port 8000
```

✅ Если видите `Uvicorn running on http://127.0.0.1:8000` — сервер работает!

### Шаг 2: Запуск Frontend (игра)

Откройте **второй** терминал:

```bash
# Перейти в папку frontend
cd frontend

# Установить зависимости (нужен Node.js)
npm install

# Запустить игру
npm run dev
```

✅ Браузер автоматически откроется на http://localhost:5173

### Шаг 3: Играть! 🎮

Откройте в браузере: **http://localhost:5173**

---

## 🎮 Управление

| Клавиша | Действие |
|---------|----------|
| `↑` `↓` `←` `→` | Движение |
| `W` `A` `S` `D` | Движение (альтернатива) |
| `SPACE` | Пауза / Старт |
| `ENTER` | Начать заново |

---

## 📁 Структура проекта

```
Snake/
├── backend/                    # 🐍 Python сервер (FastAPI)
│   ├── app/
│   │   ├── main.py            # Главный файл API
│   │   ├── database.py        # Подключение к SQLite
│   │   ├── models.py          # Структура таблиц БД
│   │   └── schemas.py         # Форматы данных API
│   ├── requirements.txt       # Python зависимости
│   └── snake.db               # База данных (создаётся автоматически)
│
├── frontend/                   # ⚛️ React приложение
│   ├── src/
│   │   ├── components/        # Компоненты игры
│   │   │   ├── Game.jsx       # Главный игровой компонент
│   │   │   └── Leaderboard.jsx # Таблица лидеров
│   │   ├── hooks/             # Логика игры
│   │   │   ├── useSnake.js    # Логика змейки
│   │   │   └── useGameLoop.js # Игровой цикл
│   │   ├── api/               # Общение с backend
│   │   │   └── scores.js      # API вызовы
│   │   ├── styles/            # CSS стили
│   │   └── App.jsx            # Главный компонент
│   ├── package.json           # JavaScript зависимости
│   └── vite.config.js         # Настройки сборки
│
├── Docs/                       # 📚 Документация
├── .cursorrules               # Правила для Cursor AI
├── .gitignore                 # Игнорируемые файлы Git
├── .flake8                    # Настройки линтера Python
└── README.md                  # Этот файл
```

---

## 🔧 API Endpoints

Backend предоставляет REST API:

| Метод | URL | Описание |
|-------|-----|----------|
| `GET` | `/api/health` | Проверка работы сервера |
| `POST` | `/api/scores` | Сохранить результат |
| `GET` | `/api/scores` | Таблица лидеров |
| `GET` | `/api/scores/best` | Лучший результат |
| `DELETE` | `/api/scores` | Очистить все результаты |

📖 Полная документация API: http://localhost:8000/docs (после запуска backend)

---

## 🛠️ Для разработчиков

### Проверка кода Python
```bash
cd backend
flake8 .
```

### Структура базы данных

Файл `snake.db` (SQLite) содержит таблицу `scores`:

| Поле | Тип | Описание |
|------|-----|----------|
| id | INTEGER | Уникальный ID |
| player_name | VARCHAR(100) | Имя игрока |
| score | INTEGER | Набранные очки |
| played_at | DATETIME | Дата и время игры |

---

## ❓ Частые проблемы

### Backend не запускается

```
Error: command 'uvicorn' not found
```
**Решение:** Убедитесь, что виртуальное окружение активировано:
```bash
# Windows
.\venv\Scripts\Activate.ps1
```

### Frontend показывает ошибку CORS

**Решение:** Убедитесь, что backend запущен на порту 8000.

### Таблица лидеров не загружается

**Решение:** Проверьте, что backend сервер запущен.

---

## 🎨 Дизайн

- **Стиль:** Neon/Cyberpunk
- **Цвета:** Тёмный фон, неоновый зелёный, розовый акцент
- **Шрифты:** Orbitron (заголовки), Rajdhani (текст), Press Start 2P (счёт)

---

## 📝 Лицензия

MIT — делайте что хотите!

---

## 🙏 Благодарности

Создано с помощью:
- [FastAPI](https://fastapi.tiangolo.com/) — Python веб-фреймворк
- [React](https://react.dev/) — JavaScript UI библиотека
- [Vite](https://vitejs.dev/) — Сборщик фронтенда
- [Cursor AI](https://cursor.sh/) — AI-помощник для разработки

---

*🐍 Приятной игры!*
